name: Auto Release – wysiwyg-rails

on:
  pull_request:
    types: [closed]
    branches:
      - master

permissions:
  contents: write

jobs:
  release:
    # Run only when PR is merged
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest

    steps:
      # --------------------------------------------
      # Validate release intent
      # --------------------------------------------
      - name: Validate release intent
        id: intent
        uses: actions/github-script@v6
        with:
          script: |
            const body  = context.payload.pull_request.body || '';
            const title = context.payload.pull_request.title || '';
            const text  = `${title}\n${body}`;

            core.setOutput(
              'release',
              /release:\s*yes/i.test(text) ? 'true' : 'false'
            );

      - name: Stop if not a release PR
        if: steps.intent.outputs.release == 'false'
        run: |
          echo "ℹ️ Not a release PR. Skipping auto-release."
          exit 0

      # --------------------------------------------
      # Checkout merged code
      # --------------------------------------------
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT }}
          fetch-depth: 0

      # --------------------------------------------
      # Setup Ruby
      # --------------------------------------------
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.1'

      # --------------------------------------------
      # Read version from version.rb (single source)
      # --------------------------------------------
      - name: Read version from version.rb
        run: |
          VERSION=$(ruby -e "require './lib/wysiwyg-rails/version'; puts WYSIWYG::Rails::VERSION")

          if [ -z "$VERSION" ]; then
            echo "❌ Failed to read version from version.rb"
            exit 1
          fi

          if ! [[ "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z.-]+)?$ ]]; then
            echo "❌ Invalid version format: $VERSION"
            exit 1
          fi

          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "✅ Releasing version $VERSION"

      # --------------------------------------------
      # Prepare clean release notes
      # --------------------------------------------
      - name: Prepare release notes
        run: |
          PR_BODY="${{ github.event.pull_request.body }}"

          CLEAN_NOTES=$(echo "$PR_BODY" | sed \
            -e '/^release:\s*/Id' \
            -e '/^## Release Notes.*$/Id')

          echo "RELEASE_NOTES<<EOF" >> $GITHUB_ENV
          echo "$CLEAN_NOTES" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV

      # --------------------------------------------
      # Create GitHub Release (NO GEM ATTACHED)
      # --------------------------------------------
      - name: Create GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.PAT }}
        run: |
          gh release create "v${VERSION}" \
            --title "Release ${VERSION}" \
            --notes "$RELEASE_NOTES" \
            --latest

      # --------------------------------------------
      # Build gem (for validation / future RubyGems flow)
      # --------------------------------------------
      - name: Build gem
        run: gem build wysiwyg-rails.gemspec
